# Configuración para desarrollo local
# Copia este contenido a un archivo llamado .env.local en la raíz del proyecto

# MongoDB Local - Base de datos para desarrollo y testing
# Esta base de datos es independiente de la del VPS
MONGODB_URI=mongodb://127.0.0.1:27017/mini-crm-dev

# Si MongoDB local tiene autenticación habilitada, usa:
# MONGODB_URI=mongodb://usuario:password@127.0.0.1:27017/mini-crm-dev?authSource=admin

# Para usar la base de datos del VPS (solo en producción):
# MONGODB_URI=mongodb://usuario:password@servidor-vps:27017/mini-crm?authSource=admin

# URL base de la aplicación (opcional)
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

